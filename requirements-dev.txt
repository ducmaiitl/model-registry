# Dev/test environment. Installs the package editable plus the [dev] extras
# (full mlflow for the sqlite test backend, pytest, huggingface_hub).
# Server-side deps (psycopg2, google-cloud-storage) live in requirements.txt
# and only inside the MLflow image.
-e .[dev]
